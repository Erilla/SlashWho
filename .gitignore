# Local environment and secrets
.env
.env.*
!.env.example

# Logs and temporary files
*.log
*.tmp
temp/
tmp/
.superpowers/
.worktrees/

# Operating-system metadata
.DS_Store
Thumbs.db
Desktop.ini

# Dependencies and build output
node_modules/
.next/
coverage/
dist/
playwright-report/
test-results/

# Regenerated by every `next dev` and `next build`, and the two write
# different paths into it, so tracking it produces a diff that flips back and
# forth according to which command the author ran last.
apps/web/next-env.d.ts
