# Domain Docs

Before exploring the codebase, read `CONTEXT.md` and any relevant ADRs under `docs/adr/`. If they do not exist, proceed silently; domain-modeling skills create them lazily when useful.

This repository uses a single-context layout:

```
/
├── CONTEXT.md
├── docs/adr/
└── src/
```

Use terminology defined in `CONTEXT.md`. Explicitly flag any proposed change that contradicts an existing ADR.
